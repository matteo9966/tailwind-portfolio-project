import { SlideInWhenVisibleDirective } from './slide-in-when-visible.directive';

describe('SlideInWhenVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideInWhenVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
