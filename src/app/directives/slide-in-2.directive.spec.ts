import { SlideIn2Directive } from './slide-in-2.directive';

describe('SlideIn2Directive', () => {
  it('should create an instance', () => {
    const directive = new SlideIn2Directive();
    expect(directive).toBeTruthy();
  });
});
